<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Welcome to example.com, <%= @order.email %></h1>
    <p>
      You have successfully ordered online,
      your order number is: <%= @order.id %>.<br>
    </p>
   <table class="table table-bordered">
      <thead>
        <tr>
          <th>Image</th>
          <th> Name </th>
          <th> Description </th>
          <th>Quantity</th>
          <th> Line Item Totals </th>
        </tr>
      </thead>
      <tbody>
          <% @order.line_items.each do |item| %>
        <tr>
            <td>
            <%= image_tag item.product.image.tiny%>
            </td>
            <td>
            <%= item.product.name %>
            </td>
            <td> <%= item.product.description %> </td>
            <td> <%= item.quantity %> </td>
            <td><%= item.total_price %></td>
        </tr>
          <% end %>
        
      </tbody>
      <tfoot>
        <tr>
          <th colspan="4">
            TOTAL: 
          </th>
          <th>
            <%= @order.line_items.sum(:total_price_cents)/100.00 %>
          </th>
        </tr>
      </tfoot>
    </table>
    <p>Thanks for ordering and have a great day!</p>
  </body>
</html>